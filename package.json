{
  "name": "loadbalancer-node",
  "version": "1.0.0",
  "description": "Simple node for load balancer demo",
  "main": "app.js",
  "scripts": {
    "start": "concurrently \"node app.js\" \"node fix-server.js\"",
    "fix": "node fix-server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}